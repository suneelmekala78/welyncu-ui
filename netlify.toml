# Netlify configuration for WeLyncu Client
[build]
  command = "CI=false npm run build"
  publish = "build"

# SPA routing — redirect all requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  CI = "false"
